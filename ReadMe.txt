This app is created as a showcase for IBM internship position
I used python/django

Create account. Log in. A search field should register your search text, save it in database and
represent the last 5 searched text and date-time. I used postgresql database.

before running app
pip install -r requirements.txt

to run the app
python manage.py runserver

